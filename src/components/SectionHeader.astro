---
interface Props {
  title: string;
  subtitle: string;
}

const { title, subtitle } = Astro.props;
---

<div class="flex flex-col items-center gap-3 max-w-[680px] text-center px-2">
  <h2 class="font-heading text-2xl md:text-[32px] font-bold text-text-primary">{title}</h2>
  <p class="font-body text-base md:text-lg text-text-secondary leading-[1.6]">{subtitle}</p>
</div>
